@model IEnumerable<PesanMakan.Models.Menu>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Nama Menu")
            </th>
            <th>
                @Html.DisplayName("Harga Menu")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaMenu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HargaMenu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMenu"><i class="fa fa-marker fa-lg"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdMenu"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdMenu"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
